@model BuahSayur.ViewModel.CustomerViewModel


<div>
    <h4>Delete This Customer from list?</h4>
    <hr />
    <dl class="dl-horizontal">
      
        <dt>
            @Html.DisplayNameFor(model => model.FullName)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.FullName)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.Address)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Address)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.PhoneNumber)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.PhoneNumber)
        </dd>
        <dt>
            @Html.DisplayNameFor(model => model.Province_Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.Province_Name)
        </dd>       

        <dt>
            @Html.DisplayNameFor(model => model.City_Name)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.City_Name)
        </dd>

        <dt>
            @Html.DisplayNameFor(model => model.IsActivated)
        </dt>

        <dd>
            @Html.DisplayFor(model => model.IsActivated)
        </dd>

    </dl>

    @using (Html.BeginForm()) {
        @Html.AntiForgeryToken()

        <div class="form-actions no-color modal-footer">
            <input type="button" value="Delete" class="btn btn-primary btn-delete-conf" data-id="@Model.Id" />
            <input type="button" value="Cancel" class="btn btn-default" data-dismiss="modal" />
        </div>
    }
</div>

<script type="text/javascript">
    $(".btn-delete-conf").click(function () {
        $.ajax({
            url: '@Url.Action("DeleteConfirm", "Customer")',
            type: 'post',
            data: { id: $(this).attr("data-id") },
            success: function(result){
                if (result.success) {
                    $("#modal-form").modal("hide");
                    ShowList();
                    SuccessDeleted();
                } else {
                    alert('Error: ' + result.message);
                }
            }
        });
    });
</script>
